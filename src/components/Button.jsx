const Button = ({ text }) => {
    return (
        <button className="px-2 py-1 mx-1 my-2 border-black border-2 rounded-lg bg-slate-200">
            {text}
        </button>
    );
};

export default Button;
